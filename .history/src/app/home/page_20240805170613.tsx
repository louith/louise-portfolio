import { colors } from "@/lib/colors";
import { Box, Text } from "@chakra-ui/react";
import React from "react";

const Home = () => {
  return (
    <Box w={"100%"} h={"100%"} bgColor={colors.font}>
      <Text>Home</Text>
    </Box>
  );
};

export default Home;
